const BASE: string = "https://dummyjson.com/products";
const API_CATEGORIES = `${BASE}/categories`;
const API_PRODUCTS_BY_CATEGORY = `${BASE}/category`;

export {
    BASE,
    API_CATEGORIES,
    API_PRODUCTS_BY_CATEGORY
}